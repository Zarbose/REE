#DEPRECATED
# #/bin/bash -u

# LOG_FILE="/var/log/REE.log"

# # Fonction pour ajouter un timestamp aux messages de logs
# log() {
#     echo "$(date '+%Y-%m-%d %H:%M:%S') - $1" >> "$LOG_FILE"
# }
# #ecriture du cronjob uniquement dans les raspberry qui ne sont pas la première
# echo "* * * * * root sleep 5 && check_ip" >> /etc/cron.d/e2scrub_all
# log "Ecriture du cronjob dans /etc/cron.d/e2scrub_all  "